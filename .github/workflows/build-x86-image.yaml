name: Build x86 Image

run-name: ${{ github.event_name == 'workflow_dispatch' && format('x86-e2e pr={0} head={1} approval={2} generation={3} groups={4} full={5}', inputs.prNumber, inputs.headSHA, inputs.approvalGeneration, inputs.dispatchGeneration, inputs.requestedGroupNames, inputs.full && '1' || '0') || '' }}

permissions:
  contents: read
  packages: read

defaults:
  run:
    shell: >-
      /usr/bin/env -u ACTIONS_RUNTIME_TOKEN -u ACTIONS_RUNTIME_URL -u ACTIONS_RESULTS_URL
      -u ACTIONS_CACHE_URL -u ACTIONS_ID_TOKEN_REQUEST_TOKEN -u ACTIONS_ID_TOKEN_REQUEST_URL
      -u LD_PRELOAD -u LD_LIBRARY_PATH -u BASH_ENV -u ENV -u PYTHONPATH -u PYTHONHOME
      -u NODE_OPTIONS -u PERL5OPT -u RUBYOPT -u GIT_CONFIG_COUNT
      python3 -c "import os,pathlib,shutil,subprocess,sys,tempfile;
      realEnv=os.environ['GITHUB_ENV']; shadowDir=tempfile.mkdtemp();
      os.environ['GITHUB_ENV']=shadowDir+'/env'; os.environ['GITHUB_PATH']=shadowDir+'/path';
      pathlib.Path(os.environ['GITHUB_ENV']).touch(); pathlib.Path(os.environ['GITHUB_PATH']).touch();
      result=subprocess.run(['bash','--noprofile','--norc','-e','-o','pipefail',sys.argv[1]],env=os.environ);
      allowed=('TAG','GO_VERSION','E2E_DIR','VERSION','DEBUG_WRAPPER');
      lines=[line for line in pathlib.Path(os.environ['GITHUB_ENV']).read_text().splitlines() if line.partition('=')[0] in allowed];
      open(realEnv,'a').write(('\n'.join(lines)+'\n') if lines else '');
      shutil.rmtree(shadowDir); sys.exit(result.returncode)" {0}

on:
  pull_request:
    branches:
    - main
    - release-*
    paths-ignore:
    - 'docs/**'
    - '**.md'
  push:
    branches:
    - main
    - release-*
    paths-ignore:
    - 'docs/**'
    - '**.md'
  workflow_dispatch:
    inputs:
      prNumber:
        description: Open pull request number
        required: true
        type: number
      headSHA:
        description: Exact current pull request HEAD SHA
        required: true
        type: string
      baseSHA:
        description: Exact trusted pull request base SHA
        required: true
        type: string
      approvalGeneration:
        description: Durable authorized comment generation
        required: true
        type: number
      dispatchGeneration:
        description: Unique trusted reducer run generation
        required: true
        type: number
      requestedGroups:
        description: JSON array of explicitly requested stable groups
        required: true
        default: '[]'
        type: string
      requestedGroupNames:
        description: Canonical comma-separated groups, or a single dash
        required: true
        default: '-'
        type: string
      full:
        description: Run the complete x86 E2E suite
        required: true
        default: false
        type: boolean
      requestKey:
        description: Dispatcher-generated idempotency key
        required: true
        type: string
      catalogRevision:
        description: Trusted E2E catalog revision bound by the dispatcher
        required: true
        type: string

concurrency:
  group: ${{ github.event_name == 'workflow_dispatch' && format('{0}-{1}', github.workflow, inputs.requestKey) || format('{0}-{1}', github.workflow, github.ref) }}
  cancel-in-progress: true

env:
  # renovate: datasource=github-releases depName=kind packageName=kubernetes-sigs/kind
  KIND_VERSION: v0.32.0
  # renovate: datasource=github-releases depName=golangci-lint packageName=golangci/golangci-lint
  GOLANGCI_LINT_VERSION: v2.12.2
  # renovate: datasource=github-releases depName=helm packageName=helm/helm
  HELM_VERSION: v4.2.4
  # renovate: datasource=github-releases depName=submariner packageName=submariner-io/submariner
  SUBMARINER_VERSION: 0.24.0
  # renovate: datasource=github-releases depName=talos packageName=siderolabs/talos
  TALOS_VERSION: v1.13.8

jobs:
  e2e-selection:
    name: x86 E2E Selection
    if: github.event_name != 'workflow_dispatch' || github.actor == 'github-actions[bot]'
    continue-on-error: ${{ github.event_name != 'workflow_dispatch' }}
    runs-on: ubuntu-24.04
    env:
      GH_TOKEN: ${{ github.event_name == 'workflow_dispatch' && github.token || '' }}
      PYTHONDONTWRITEBYTECODE: "1"
    outputs:
      executionJobIds: ${{ steps.outputs.outputs.executionJobIds }}
      k8sConformanceMatrix: ${{ steps.outputs.outputs.k8sConformanceMatrix || steps.fallbackMatrices.outputs.k8sConformanceMatrix }}
      kubeOvnConformanceMatrix: ${{ steps.outputs.outputs.kubeOvnConformanceMatrix || steps.fallbackMatrices.outputs.kubeOvnConformanceMatrix }}
      headSHA: ${{ steps.context.outputs.headSHA }}
      prNumber: ${{ steps.context.outputs.prNumber }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.baseSHA || github.sha }}
          persist-credentials: false
          fetch-depth: 0

      - name: Initialize fail-closed conformance matrices
        id: fallbackMatrices
        if: always()
        run: |
          matrix='{"include":[{"ip-family":"ipv4","mode":"overlay"},{"ip-family":"ipv4","mode":"underlay"},{"ip-family":"ipv6","mode":"overlay"},{"ip-family":"ipv6","mode":"underlay"},{"ip-family":"dual","mode":"overlay"},{"ip-family":"dual","mode":"underlay"}]}'
          echo "k8sConformanceMatrix=$matrix" >> "$GITHUB_OUTPUT"
          echo "kubeOvnConformanceMatrix=$matrix" >> "$GITHUB_OUTPUT"

      - name: Collect trusted selection context
        id: context
        env:
          EVENT_NAME: ${{ github.event_name }}
          INPUT_HEAD_SHA: ${{ inputs.headSHA }}
          INPUT_BASE_SHA: ${{ inputs.baseSHA }}
          INPUT_PR_NUMBER: ${{ inputs.prNumber }}
          INPUT_REQUESTED_GROUPS: ${{ inputs.requestedGroups }}
          INPUT_REQUESTED_GROUP_NAMES: ${{ inputs.requestedGroupNames }}
          INPUT_CATALOG_REVISION: ${{ inputs.catalogRevision }}
          INPUT_FULL: ${{ inputs.full }}
          PR_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail
          printf '{}' > selector-event.json
          : > changed-paths.txt
          forceFull=false
          forceFullReason=''
          requestedGroups='[]'
          headSHA="$GITHUB_SHA"
          prNumber=0
          checkedOutSHA=$(git rev-parse HEAD)
          if [ "$EVENT_NAME" = workflow_dispatch ] && [ "$checkedOutSHA" != "$INPUT_BASE_SHA" ]; then
            echo 'trusted selector checkout does not match the approved base revision' >&2
            exit 1
          fi
          if [ "$EVENT_NAME" = pull_request ]; then
            git diff --name-status --diff-filter=ACDMRT -z -M \
              "$PR_BASE_SHA...$PR_HEAD_SHA" > changed-name-status.bin
            python3 - <<'PY'
          import sys
          from pathlib import Path

          sys.path.insert(0, "hack")
          import e2e_selector as e2eSelector

          paths = e2eSelector.changedPathsFromNameStatus(
              Path("changed-name-status.bin").read_bytes()
          )
          Path("changed-paths.txt").write_bytes(
              b"".join(path.encode("utf-8", errors="surrogateescape") + b"\0" for path in paths)
          )
          PY
            cp "$GITHUB_EVENT_PATH" selector-event.json
            python3 - <<'PY'
          import json
          from pathlib import Path

          event = json.loads(Path("selector-event.json").read_text())
          event["pull_request"]["labels"] = []
          Path("selector-event.json").write_text(json.dumps(event))
          PY
            headSHA="$PR_HEAD_SHA"
            prNumber="$PR_NUMBER"
          elif [ "$EVENT_NAME" = workflow_dispatch ]; then
            gh api "repos/$GITHUB_REPOSITORY/pulls/$INPUT_PR_NUMBER" > pull-request.json
            python3 - "$INPUT_HEAD_SHA" "$INPUT_BASE_SHA" "$GITHUB_REF_NAME" "$GITHUB_SHA" <<'PY'
          import json
          import re
          import sys
          from pathlib import Path

          pullRequest = json.loads(Path("pull-request.json").read_text())
          if pullRequest.get("state") != "open":
              raise SystemExit("pull request is not open")
          if not re.fullmatch(r"[0-9a-f]{40}", sys.argv[1]):
              raise SystemExit("invalid requested HEAD SHA")
          if pullRequest["head"]["sha"] != sys.argv[1]:
              raise SystemExit("pull request HEAD changed; dispatch again")
          if pullRequest["base"]["sha"] != sys.argv[2]:
              raise SystemExit("pull request base revision changed; dispatch again")
          if pullRequest["base"]["ref"] != sys.argv[3]:
              raise SystemExit("executor must run from the pull request base branch")
          if sys.argv[4] != sys.argv[2]:
              raise SystemExit("executor workflow revision does not match the approved base")
          pullRequest["labels"] = []
          Path("selector-event.json").write_text(json.dumps({"pull_request": pullRequest}))
          PY
            gh api --paginate --slurp \
              "repos/$GITHUB_REPOSITORY/pulls/$INPUT_PR_NUMBER/files?per_page=100" > pull-request-files.json
            python3 - <<'PY'
          import json
          from pathlib import Path

          pullRequest = json.loads(Path("pull-request.json").read_text())
          pages = json.loads(Path("pull-request-files.json").read_text())
          changedFiles = [changedFile for page in pages for changedFile in page]
          expectedCount = pullRequest.get("changed_files")
          if (
              not isinstance(expectedCount, int)
              or expectedCount >= 3000
              or expectedCount != len(changedFiles)
          ):
              Path("force-full-reason.txt").write_text(
                  "pull request file list is incomplete; the full suite is required"
              )
          with Path("changed-paths.txt").open("wb") as stream:
              for changedFile in changedFiles:
                  for field in ("previous_filename", "filename"):
                      if field in changedFile:
                          stream.write(changedFile[field].encode() + b"\0")
          PY
            if [ -s force-full-reason.txt ]; then
              forceFullReason=$(cat force-full-reason.txt)
            fi
            python3 - "$INPUT_REQUESTED_GROUPS" "$INPUT_REQUESTED_GROUP_NAMES" "$INPUT_CATALOG_REVISION" <<'PY'
          import hashlib
          import json
          import sys
          from pathlib import Path

          groups = json.loads(sys.argv[1])
          if not isinstance(groups, list) or any(not isinstance(group, str) for group in groups):
              raise SystemExit("requestedGroups must be a JSON array of strings")
          canonicalGroups = sorted(set(groups))
          catalog = json.loads(Path(".github/e2e-selection.json").read_text())
          catalogRevision = hashlib.sha256(
              json.dumps(catalog, sort_keys=True, separators=(",", ":")).encode()
          ).hexdigest()
          if catalogRevision != sys.argv[3]:
              raise SystemExit("executor catalog revision does not match dispatcher")
          unknownGroups = sorted(set(canonicalGroups) - set(catalog["groups"]))
          if unknownGroups:
              raise SystemExit(f"unknown requested E2E group: {unknownGroups[0]}")
          canonical = ",".join(canonicalGroups) or "-"
          if canonical != sys.argv[2]:
              raise SystemExit("requested group names do not match requestedGroups")
          PY
            headSHA="$INPUT_HEAD_SHA"
            prNumber="$INPUT_PR_NUMBER"
            requestedGroups="$INPUT_REQUESTED_GROUPS"
            forceFull="$INPUT_FULL"
          else
            forceFull=true
          fi
          echo "headSHA=$headSHA" >> "$GITHUB_OUTPUT"
          echo "prNumber=$prNumber" >> "$GITHUB_OUTPUT"
          echo "forceFull=$forceFull" >> "$GITHUB_OUTPUT"
          echo "forceFullReason=$forceFullReason" >> "$GITHUB_OUTPUT"
          echo "requestedGroups=$requestedGroups" >> "$GITHUB_OUTPUT"

      - name: Compute the selection plan
        env:
          FORCE_FULL: ${{ steps.context.outputs.forceFull }}
          FORCE_FULL_REASON: ${{ steps.context.outputs.forceFullReason }}
          HEAD_SHA: ${{ steps.context.outputs.headSHA }}
          REQUESTED_GROUPS: ${{ steps.context.outputs.requestedGroups }}
        run: |
          args=(
            --paths-file changed-paths.txt
            --event-file selector-event.json
            --request-groups-json "$REQUESTED_GROUPS"
            --head-sha "$HEAD_SHA"
            --force-full-reason "$FORCE_FULL_REASON"
            --plan-file e2e-selection-plan.json
            --summary-file e2e-selection-summary.md
          )
          if [ "$FORCE_FULL" = true ]; then
            args+=(--label e2e:full)
          fi
          python3 hack/e2e_selector.py "${args[@]}"
          cat e2e-selection-summary.md >> "$GITHUB_STEP_SUMMARY"

      - name: Export executor job IDs
        id: outputs
        env:
          EVENT_NAME: ${{ github.event_name }}
        run: |
          python3 - <<'PY'
          import json
          import os
          import sys
          from pathlib import Path

          sys.path.insert(0, "hack")
          import e2e_selector as e2eSelector

          plan = json.loads(Path("e2e-selection-plan.json").read_text())
          if os.environ["EVENT_NAME"] == "workflow_dispatch":
              executionPlan = plan
          else:
              workflow = Path(".github/workflows/build-x86-image.yaml").read_text()
              executionPlan = {"matrix": e2eSelector.expandWorkflow(workflow)}
          jobIds = sorted({entry["job"] for entry in executionPlan["matrix"]})
          matrices = {
              "k8sConformanceMatrix": e2eSelector.jobMatrix(
                  executionPlan,
                  "k8s-conformance-e2e",
              ),
              "kubeOvnConformanceMatrix": e2eSelector.jobMatrix(
                  executionPlan,
                  "fabric-conformance-e2e",
              ),
          }
          with Path(os.environ["GITHUB_OUTPUT"]).open("a") as stream:
              stream.write("executionJobIds=" + json.dumps(jobIds, separators=(",", ":")) + "\n")
              for name, matrix in matrices.items():
                  stream.write(name + "=" + json.dumps(matrix, separators=(",", ":")) + "\n")
          PY

      - name: Validate the E2E control modules
        run: python3 -m unittest hack/test_e2e_selector.py hack/test_e2e_control.py

      - name: Upload the selection plan
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: x86-e2e-selection-${{ steps.context.outputs.prNumber }}-${{ steps.context.outputs.headSHA }}-${{ github.run_id }}-${{ github.run_attempt }}
          path: |
            changed-paths.txt
            e2e-selection-plan.json
            e2e-selection-summary.md
          retention-days: 30

  build-kube-ovn-base:
    name: Build kube-ovn-base
    if: github.event_name != 'workflow_dispatch' || github.actor == 'github-actions[bot]'
    runs-on: ubuntu-24.04
    outputs:
      build-base: ${{ steps.check.outputs.build-base }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false
          fetch-depth: 2

      - id: check
        run: |
          if [ ${{ github.event_name }} != 'pull_request' ]; then
            exit
          fi
          tmp_dir=`mktemp -d`
          cat > "$tmp_dir/on_changes.txt" <<EOF
          dist/images/Dockerfile.base
          dist/images/OpenBFDD-compile.patch
          dist/images/patches/
          dist/images/go-deps/download-go-deps.sh
          dist/images/go-deps/rebuild-go-deps.sh
          EOF
          if git diff --name-only HEAD^ HEAD | grep -Ff "$tmp_dir/on_changes.txt"; then
            echo build-base=1 >> "$GITHUB_OUTPUT"
          fi
          rm -frv "$tmp_dir"

      - name: Free disk space
        if: steps.check.outputs.build-base == 1
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: docker/setup-buildx-action@v4
        if: steps.check.outputs.build-base == 1
        with:
          driver-opts: env.BUILDKIT_STEP_LOG_MAX_SIZE=50000000

      - uses: actions/setup-go@v7
        if: steps.check.outputs.build-base == 1
        id: setup-go
        with:
          go-version-file: go.mod
          check-latest: true
          cache: false

      - name: Build kube-ovn-base image
        id: build
        if: steps.check.outputs.build-base == 1
        env:
          GO_VERSION: ${{ steps.setup-go.outputs.go-version }}
        run: |
            make base-amd64
            make base-tar-amd64

      - name: Upload base images to artifact
        if: steps.check.outputs.build-base == 1
        uses: actions/upload-artifact@v7
        with:
          name: kube-ovn-base
          path: image-amd64.tar

  build-fabric-dpdk-base:
    name: Build fabric-dpdk-base
    if: github.event_name != 'workflow_dispatch' || github.actor == 'github-actions[bot]'
    runs-on: ubuntu-24.04
    outputs:
      build-dpdk-base: ${{ steps.check.outputs.build-dpdk-base }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false
          fetch-depth: 2

      - id: check
        run: |
          if [ ${{ github.event_name }} != 'pull_request' ]; then
            exit
          fi
          tmp_dir=`mktemp -d`
          cat > "$tmp_dir/on_changes.txt" <<EOF
          dist/images/Dockerfile.base-dpdk
          dist/images/patches/
          EOF
          if git diff --name-only HEAD^ HEAD | grep -Ff "$tmp_dir/on_changes.txt"; then
            echo build-dpdk-base=1 >> "$GITHUB_OUTPUT"
          fi
          rm -frv "$tmp_dir"

      - name: Free disk space
        if: steps.check.outputs.build-dpdk-base == 1
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: docker/setup-buildx-action@v4
        if: steps.check.outputs.build-dpdk-base == 1

      - name: Build fabric-dpdk-base image
        id: build
        if: steps.check.outputs.build-dpdk-base == 1
        run: |
          make base-amd64-dpdk
          make base-tar-amd64-dpdk

      - name: Upload dpdk base images to artifact
        if: steps.check.outputs.build-dpdk-base == 1
        uses: actions/upload-artifact@v7
        with:
          name: fabric-dpdk-base
          path: image-amd64-dpdk.tar

  lint-ut:
    name: Lint and unit test
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false
      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: go.mod
          check-latest: true
          cache: false

      - name: Setup environment variables
        run: |
          echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        if: github.event_name == 'push'
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-lint-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-lint-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-

      - name: Unit test
        run: make ut

      - uses: shogo82148/actions-goveralls@v1
        continue-on-error: true
        with:
          path-to-profile: profile.cov

      - name: Install golangci-lint
        run: |
          curl -sSfL https://golangci-lint.run/install.sh | sh -s -- -b $(go env GOPATH)/bin $GOLANGCI_LINT_VERSION

      - name: Lint
        run: |
          go mod tidy
          git diff --exit-code -- go.mod go.sum
          make verify-crd
          make verify-version
          make lint

  build-fabric-dpdk:
    name: Build fabric dpdk
    runs-on: ubuntu-24.04
    if: github.event_name != 'pull_request'
    needs:
      - build-fabric-dpdk-base
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false
      - uses: docker/setup-buildx-action@v4
      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: go.mod
          check-latest: true
          cache: false

      - name: Setup environment variables
        run: |
          echo "TAG=$(cat VERSION)" >> "$GITHUB_ENV"
          echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Download dpdk base images
        if: needs.build-fabric-dpdk-base.outputs.build-dpdk-base == 1
        uses: actions/download-artifact@v8
        with:
          name: fabric-dpdk-base

      - name: Load dpdk base images
        if: needs.build-fabric-dpdk-base.outputs.build-dpdk-base == 1
        run: |
          docker load --input image-amd64-dpdk.tar
          docker tag kubeovn/kube-ovn-base:$TAG-amd64-dpdk kubeovn/kube-ovn-base:$TAG-dpdk

      - name: Pull dpdk base image
        if: needs.build-fabric-dpdk-base.outputs.build-dpdk-base != 1
        run: make pull-base-dpdk

      - name: Build dpdk
        run: |
          if [ ${{ needs.build-fabric-dpdk-base.outputs.build-dpdk-base || 0 }} = 1 ]; then
            make build-fabric-dpdk
          else
            make image-fabric-dpdk
          fi
          make tar-fabric-dpdk

      - name: Upload dpdk images to artifact
        uses: actions/upload-artifact@v7
        with:
          name: fabric-dpdk
          path: fabric-dpdk.tar

  build-fabric:
    name: Build fabric
    runs-on: ubuntu-24.04
    needs:
      - e2e-selection
      - build-kube-ovn-base
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false
      - uses: docker/setup-buildx-action@v4
      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: go.mod
          check-latest: true
          cache: false

      - name: Setup environment variables
        run: |
          echo "TAG=$(cat VERSION)" >> "$GITHUB_ENV"
          echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        if: github.event_name == 'push'
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Download base images
        if: needs.build-kube-ovn-base.outputs.build-base == 1
        uses: actions/download-artifact@v8
        with:
          name: kube-ovn-base

      - name: Load base images
        if: needs.build-kube-ovn-base.outputs.build-base == 1
        run: |
          docker load --input image-amd64.tar
          docker tag kubeovn/kube-ovn-base:$TAG-amd64 kubeovn/kube-ovn-base:$TAG
          docker tag kubeovn/kube-ovn-base:$TAG-debug-amd64 kubeovn/kube-ovn-base:$TAG-debug

      - name: Pull base images
        if: needs.build-kube-ovn-base.outputs.build-base != 1
        run: make pull-base

      - name: Scan base image
        uses: aquasecurity/trivy-action@v0.36.0
        env:
          TRIVY_DB_REPOSITORY: public.ecr.aws/aquasecurity/trivy-db:2
        with:
          scan-type: image
          scanners: vuln
          image-ref: docker.io/kubeovn/kube-ovn-base:${{ env.TAG }}
          format: json
          output: trivy-result.json
          ignore-unfixed: true
          trivyignores: .trivyignore
          vuln-type: library

      - name: Build kubectl and CNI plugins from source
        env:
          CGO_ENABLED: "0"
          GO_INSTALL: "go install -v -mod=mod -trimpath"
        run: |
          go mod tidy
          cat trivy-result.json
          dockerfile=${{ github.workspace }}/dist/images/Dockerfile
          export GOBIN=`dirname "$dockerfile"`
          cni_plugins_version=`go list -m -f '{{.Version}}' github.com/containernetworking/plugins`
          cni_plugins_build_flags="-ldflags '-extldflags -static -X github.com/containernetworking/plugins/pkg/utils/buildversion.BuildVersion=$cni_plugins_version'"
          jq -r '.Results[] | select((.Type=="gobinary") and (.Vulnerabilities!=null)) | .Target' trivy-result.json | while read f; do
            bin=`basename $f`
            case $bin in
              loopback|macvlan|ipvlan)
                echo "Building $bin@$cni_plugins_version from source..."
                sh -c "$GO_INSTALL $cni_plugins_build_flags github.com/containernetworking/plugins/plugins/main/$bin"
                echo "COPY $bin /$f" >> "$dockerfile"
                ;;
              portmap)
                echo "Building $bin@$cni_plugins_version from source..."
                sh -c "$GO_INSTALL $cni_plugins_build_flags github.com/containernetworking/plugins/plugins/meta/$bin"
                echo "COPY $bin /$f" >> "$dockerfile"
                ;;
              kubectl)
                version=`go list -m -f '{{.Version}}' k8s.io/kubernetes`
                mod_dir=`go list -m -f '{{.Dir}}' k8s.io/kubernetes`
                source "$mod_dir/hack/lib/util.sh"
                source "$mod_dir/hack/lib/logging.sh"
                source "$mod_dir/hack/lib/version.sh"
                repo=kubernetes/kubernetes
                commit=unknown
                read type tag_sha < <(echo $(curl -s "https://api.github.com/repos/$repo/git/ref/tags/$version" |
                  jq -r '.object.type,.object.sha'))
                if [ $type = "commit" ]; then
                  commit=$tag_sha
                else
                  commit=$(curl -s "https://api.github.com/repos/$repo/git/tags/$tag_sha" | jq -r '.object.sha')
                fi
                export KUBE_GIT_COMMIT="${commit}"
                export KUBE_GIT_TREE_STATE='clean'
                export KUBE_GIT_VERSION="${version}"
                export KUBE_GIT_MAJOR=`echo $KUBE_GIT_VERSION | cut -d. -f1 | sed 's/$v//'`
                export KUBE_GIT_MINOR=`echo $KUBE_GIT_VERSION | cut -d. -f2`
                goldflags="all=$(kube::version::ldflags) -s -w"
                echo "Building $bin@$version from source..."
                $GO_INSTALL -ldflags="${goldflags}" k8s.io/kubernetes/cmd/kubectl
                echo "COPY $bin /$f" >> "$dockerfile"
                ;;
              gobgp)
                echo "Removing $bin from the image..."
                echo "RUN rm -f /$f" >> "$dockerfile"
                ;;
              *)
                ;;
            esac
          done

      - name: Build
        run: |
          go mod tidy
          git diff --exit-code -- go.mod go.sum
          if [ ${{ needs.build-kube-ovn-base.outputs.build-base || 0 }} = 1 ]; then
            make build-fabric
          else
            make image-fabric
          fi
          make tar-fabric

      - name: Upload images to artifact
        uses: actions/upload-artifact@v7
        with:
          name: fabric
          path: fabric.tar

  build-e2e-binaries:
    name: Build E2E Binaries
    needs: e2e-selection
    if: github.event_name != 'workflow_dispatch' || github.actor == 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Lookup Go cache
        id: lookup-go-cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-
          lookup-only: true

      - name: Free disk space
        if: steps.lookup-go-cache.outputs.cache-hit != 'true'
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - name: Go cache
        if: steps.lookup-go-cache.outputs.cache-hit != 'true' && github.event_name == 'push'
        uses: actions/cache@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - run: make e2e-build
        if: steps.lookup-go-cache.outputs.cache-hit != 'true'
        working-directory: ${{ env.E2E_DIR }}

  netpol-path-filter:
    name: Network Policy Path Filter
    needs: e2e-selection
    if: >-
      github.event_name != 'pull_request' &&
      (github.event_name != 'workflow_dispatch' || github.actor == 'github-actions[bot]')
    runs-on: ubuntu-24.04
    outputs:
      test-netpol: ${{ steps.filter.outputs.fabric-controller }}
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false
      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: go.mod
          check-latest: true
          cache: false

      - name: Generate path filter
        run: |
          filter=".github/path-filters.yaml"
          cat > $filter <<EOF
          fabric-controller:
          - go.mod
          - go.sum
          EOF

          sh hack/go-list.sh pkg/controller | while read f; do
            echo "- $f" | tee -a $filter
          done

      - uses: dorny/paths-filter@v4
        id: filter
        with:
          base: ${{ github.base_ref || github.ref_name }}
          filters: .github/path-filters.yaml
          list-files: csv

  k8s-conformance-e2e:
    name: Kubernetes Conformance E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'k8s-conformance-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.e2e-selection.outputs.k8sConformanceMatrix) }}
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - name: Remove DNS search domain
        run: |
          sudo sed -i '/^search/d' /etc/resolv.conf
          sudo systemctl restart docker

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Set environment variables
        run: |
          if [ $(($RANDOM%2)) -ne 0 ]; then
            # run as root and use valgrind to debug memory leak
            echo "VERSION=$(cat VERSION)-debug" >> "$GITHUB_ENV"
            echo "DEBUG_WRAPPER=valgrind" >> "$GITHUB_ENV"
          fi

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-${{ matrix.mode }}-${{ matrix.ip-family }}

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          E2E_NETWORK_MODE: ${{ matrix.mode }}
        run: make k8s-conformance-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-events.yaml
          tar zcf k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-events.tar.gz k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-events
          path: k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-audit-log
          path: k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-ko-log
          path: k8s-conformance-e2e-${{ matrix.ip-family }}-${{ matrix.mode }}-ko-log.tar.gz

      - name: Check valgrind result
        run: |
          if [ "x$DEBUG_WRAPPER" != "xvalgrind" ]; then
            exit
          fi

          kubectl -n kube-system rollout restart ds ovs-ovn
          kubectl -n kube-system rollout status ds ovs-ovn
          sleep 10
          kubectl -n kube-system rollout restart deploy ovn-central
          kubectl -n kube-system rollout status deploy ovn-central
          while true; do
            if [ $(kubectl -n kube-system get pod -l app=ovs -o name | wc -l) -eq $(kubectl get node -o name | wc -l) ]; then
              break
            fi
            sleep 1
          done
          kubectl ko log ovn
          kubectl ko log ovs

          for daemon in ovsdb-nb ovsdb-sb ovn-northd ovn-controller ovsdb-server ovs-vswitchd; do
            echo "Checking if valgrind log file for $daemon exists..."
            find kubectl-ko-log -type f -name "$daemon.valgrind.log.[[:digit:]]*" -exec false {} + && exit 1
          done

          find kubectl-ko-log -type f -name '*.valgrind.log.*' | while read f; do
            if grep -qw 'definitely lost' "$f"; then
              echo "Memory leak detected in $(basename $f | awk -F. '{print $1}')."
              echo $f
              cat "$f"
              exit 1
            fi;
          done

  k8s-netpol-e2e:
    name: Kubernetes Network Policy E2E
    if: |
      (github.event_name == 'workflow_dispatch' &&
       contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'k8s-netpol-e2e')) ||
      (github.event_name != 'workflow_dispatch' && (
        always() && !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled') &&
        (needs.netpol-path-filter.outputs.test-netpol == 1 || contains(github.event.pull_request.labels.*.name, 'network policy'))
      ))
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
      - netpol-path-filter
    runs-on: ubuntu-24.04
    timeout-minutes: 90
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
          - ipv6
          - dual
        np-enforcement:
          - standard
          - lax
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - name: Remove DNS search domain
        run: |
          sudo sed -i '/^search/d' /etc/resolv.conf
          sudo systemctl restart docker

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        env:
          NP_ENFORCEMENT: "${{ matrix.np-enforcement }}"
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Set environment variables
        run: |
          if [ $(($RANDOM%2)) -ne 0 ]; then
            # run as root and use valgrind to debug memory leak
            echo "VERSION=$(cat VERSION)-debug" >> "$GITHUB_ENV"
            echo "DEBUG_WRAPPER=valgrind" >> "$GITHUB_ENV"
          fi

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-${{ matrix.ip-family }}

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        run: make k8s-netpol-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > k8s-netpol-e2e-${{ matrix.ip-family }}-events.yaml
          tar zcf k8s-netpol-e2e-${{ matrix.ip-family }}-events.tar.gz k8s-netpol-e2e-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: k8s-netpol-e2e-${{ matrix.ip-family }}-events
          path: k8s-netpol-e2e-${{ matrix.ip-family }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf k8s-netpol-e2e-${{ matrix.ip-family }}-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: k8s-netpol-e2e-${{ matrix.ip-family }}-audit-log
          path: k8s-netpol-e2e-${{ matrix.ip-family }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz k8s-netpol-e2e-${{ matrix.ip-family }}-${{ matrix.np-enforcement }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: k8s-netpol-e2e-${{ matrix.ip-family }}-${{ matrix.np-enforcement }}-ko-log
          path: k8s-netpol-e2e-${{ matrix.ip-family }}-${{ matrix.np-enforcement }}-ko-log.tar.gz

      - name: Check valgrind result
        run: |
          if [ "x$DEBUG_WRAPPER" != "xvalgrind" ]; then
            exit
          fi

          kubectl -n kube-system rollout restart ds ovs-ovn
          kubectl -n kube-system rollout status ds ovs-ovn
          sleep 10
          kubectl -n kube-system rollout restart deploy ovn-central
          kubectl -n kube-system rollout status deploy ovn-central
          while true; do
            if [ $(kubectl -n kube-system get pod -l app=ovs -o name | wc -l) -eq $(kubectl get node -o name | wc -l) ]; then
              break
            fi
            sleep 1
          done
          kubectl ko log ovn
          kubectl ko log ovs

          for daemon in ovsdb-nb ovsdb-sb ovn-northd ovn-controller ovsdb-server ovs-vswitchd; do
            echo "Checking if valgrind log file for $daemon exists..."
            find kubectl-ko-log -type f -name "$daemon.valgrind.log.[[:digit:]]*" -exec false {} + && exit 1
          done

          find kubectl-ko-log -type f -name '*.valgrind.log.*' | while read f; do
            if grep -qw 'definitely lost' "$f"; then
              echo "Memory leak detected in $(basename $f | awk -F. '{print $1}')."
              echo $f
              cat "$f"
              exit 1
            fi;
          done

  cyclonus-netpol-e2e:
    name: Cyclonus Network Policy E2E
    if: |
      (github.event_name == 'workflow_dispatch' &&
       contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'cyclonus-netpol-e2e')) ||
      (github.event_name != 'workflow_dispatch' && (
        always() && !contains(needs.*.result, 'failure') && !contains(needs.*.result, 'cancelled') &&
        (needs.netpol-path-filter.outputs.test-netpol == 1 || contains(github.event.pull_request.labels.*.name, 'network policy'))
      ))
    needs:
      - e2e-selection
      - build-fabric
      - netpol-path-filter
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
          - ipv6
          - dual
        np-enforcement:
          - standard
          - lax
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Set environment variables
        run: |
          if [ $(($RANDOM%2)) -ne 0 ]; then
            # run as root and use valgrind to debug memory leak
            echo "VERSION=$(cat VERSION)-debug" >> "$GITHUB_ENV"
            echo "DEBUG_WRAPPER=valgrind" >> "$GITHUB_ENV"
          fi

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        env:
          NP_ENFORCEMENT: "${{ matrix.np-enforcement }}"
        id: install
        run: make kind-install-${{ matrix.ip-family }}

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        run: make cyclonus-netpol-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > cyclonus-netpol-e2e-${{ matrix.ip-family }}-events.yaml
          tar zcf cyclonus-netpol-e2e-${{ matrix.ip-family }}-events.tar.gz cyclonus-netpol-e2e-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: cyclonus-netpol-e2e-${{ matrix.ip-family }}-events
          path: cyclonus-netpol-e2e-${{ matrix.ip-family }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf cyclonus-netpol-e2e-${{ matrix.ip-family }}-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: cyclonus-netpol-e2e-${{ matrix.ip-family }}-audit-log
          path: cyclonus-netpol-e2e-${{ matrix.ip-family }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz cyclonus-netpol-e2e-${{ matrix.ip-family }}-${{ matrix.np-enforcement }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: cyclonus-netpol-e2e-${{ matrix.ip-family }}-${{ matrix.np-enforcement }}-ko-log
          path: cyclonus-netpol-e2e-${{ matrix.ip-family }}-${{ matrix.np-enforcement }}-ko-log.tar.gz

      - name: Check valgrind result
        run: |
          if [ "x$DEBUG_WRAPPER" != "xvalgrind" ]; then
            exit
          fi

          kubectl -n kube-system rollout restart ds ovs-ovn
          kubectl -n kube-system rollout status ds ovs-ovn
          sleep 10
          kubectl -n kube-system rollout restart deploy ovn-central
          kubectl -n kube-system rollout status deploy ovn-central
          while true; do
            if [ $(kubectl -n kube-system get pod -l app=ovs -o name | wc -l) -eq $(kubectl get node -o name | wc -l) ]; then
              break
            fi
            sleep 1
          done
          kubectl ko log ovn
          kubectl ko log ovs

          for daemon in ovsdb-nb ovsdb-sb ovn-northd ovn-controller ovsdb-server ovs-vswitchd; do
            echo "Checking if valgrind log file for $daemon exists..."
            find kubectl-ko-log -type f -name "$daemon.valgrind.log.[[:digit:]]*" -exec false {} + && exit 1
          done

          find kubectl-ko-log -type f -name '*.valgrind.log.*' | while read f; do
            if grep -qw 'definitely lost' "$f"; then
              echo "Memory leak detected in $(basename $f | awk -F. '{print $1}')."
              echo $f
              cat "$f"
              exit 1
            fi;
          done

  fabric-conformance-e2e:
    name: Kube-OVN Conformance E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-conformance-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 50
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.e2e-selection.outputs.kubeOvnConformanceMatrix) }}
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Set environment variables
        run: |
          if [ $(($RANDOM%2)) -ne 0 ]; then
            # run as root and use valgrind to debug memory leak
            echo "VERSION=$(cat VERSION)-debug" >> "$GITHUB_ENV"
            echo "DEBUG_WRAPPER=valgrind" >> "$GITHUB_ENV"
          fi

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-${{ matrix.mode }}-${{ matrix.ip-family }}

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          E2E_NETWORK_MODE: ${{ matrix.mode }}
        run: |
          make fabric-conformance-e2e
          make kind-install-multus
          make kind-install-kubevirt
          make fabric-kubevirt-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml
          tar zcf fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events
          path: fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-audit-log
          path: fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log
          path: fabric-conformance-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: Check valgrind result
        run: |
          if [ "x$DEBUG_WRAPPER" != "xvalgrind" ]; then
            exit
          fi

          kubectl -n kube-system rollout restart ds ovs-ovn
          kubectl -n kube-system rollout status ds ovs-ovn
          sleep 10
          kubectl -n kube-system rollout restart deploy ovn-central
          kubectl -n kube-system rollout status deploy ovn-central
          while true; do
            if [ $(kubectl -n kube-system get pod -l app=ovs -o name | wc -l) -eq $(kubectl get node -o name | wc -l) ]; then
              break
            fi
            sleep 1
          done
          kubectl ko log ovn
          kubectl ko log ovs

          for daemon in ovsdb-nb ovsdb-sb ovn-northd ovn-controller ovsdb-server ovs-vswitchd; do
            echo "Checking if valgrind log file for $daemon exists..."
            find kubectl-ko-log -type f -name "$daemon.valgrind.log.[[:digit:]]*" -exec false {} + && exit 1
          done

          find kubectl-ko-log -type f -name '*.valgrind.log.*' | while read f; do
            if grep -qw 'definitely lost' "$f"; then
              echo "Memory leak detected in $(basename $f | awk -F. '{print $1}')."
              echo $f
              cat "$f"
              exit 1
            fi;
          done

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  fabric-ic-conformance-e2e:
    name: Kube-OVN IC Conformance E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-ic-conformance-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
          - ipv6
          - dual
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind clusters
        run: |
          pipx install jinjanator
          make kind-init-ovn-ic-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-ovn-ic-${{ matrix.ip-family }}

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
        run: make fabric-ic-conformance-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          for cluster in `kind get clusters`; do
            kubectl config use-context kind-$cluster
            kubectl get events -A -o yaml > fabric-ic-conformance-e2e-${{ matrix.ip-family }}-$cluster-events.yaml
          done
          tar zcf fabric-ic-conformance-e2e-${{ matrix.ip-family }}-events.tar.gz \
            `kind get clusters | xargs -I {} echo fabric-ic-conformance-e2e-${{ matrix.ip-family }}-{}-events.yaml`

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: fabric-ic-conformance-e2e-${{ matrix.ip-family }}-events
          path: fabric-ic-conformance-e2e-${{ matrix.ip-family }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          for cluster in `kind get clusters`; do
            docker cp $cluster-control-plane:/var/log/kubernetes/kube-apiserver-audit.log \
              ./fabric-ic-conformance-e2e-${{ matrix.ip-family }}-$cluster-kube-apiserver-audit.log
          done
          tar zcvf fabric-ic-conformance-e2e-${{ matrix.ip-family }}-audit-log.tar.gz \
            `kind get clusters | xargs -I {} echo fabric-ic-conformance-e2e-${{ matrix.ip-family }}-{}-kube-apiserver-audit.log`

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: fabric-ic-conformance-e2e-${{ matrix.ip-family }}-audit-log
          path: fabric-ic-conformance-e2e-${{ matrix.ip-family }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          for cluster in `kind get clusters`; do
            kubectl config use-context kind-$cluster
            make kubectl-ko-log
            mv kubectl-ko-log.tar.gz fabric-ic-conformance-e2e-${{ matrix.ip-family }}-$cluster-ko-log.tar.gz
          done
          tar zcvf fabric-ic-conformance-e2e-${{ matrix.ip-family }}-ko-log.tar.gz \
            `kind get clusters | xargs -I {} echo fabric-ic-conformance-e2e-${{ matrix.ip-family }}-{}-ko-log.tar.gz`

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-ic-conformance-e2e-${{ matrix.ip-family }}-ko-log
          path: fabric-ic-conformance-e2e-${{ matrix.ip-family }}-ko-log.tar.gz

  multus-conformance-e2e:
    name: Multus Conformance E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'multus-conformance-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
          - ipv6
          - dual
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-${{ matrix.ip-family }}

      - name: Install Multus
        run: make kind-install-multus

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
        run: make fabric-multus-conformance-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > multus-conformance-e2e-${{ matrix.ip-family }}-events.yaml
          tar zcf multus-conformance-e2e-${{ matrix.ip-family }}-events.tar.gz multus-conformance-e2e-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: multus-conformance-e2e-${{ matrix.ip-family }}-events
          path: multus-conformance-e2e-${{ matrix.ip-family }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf multus-conformance-e2e-${{ matrix.ip-family }}-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: multus-conformance-e2e-${{ matrix.ip-family }}-audit-log
          path: multus-conformance-e2e-${{ matrix.ip-family }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz multus-conformance-e2e-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: multus-conformance-e2e-${{ matrix.ip-family }}-ko-log
          path: multus-conformance-e2e-${{ matrix.ip-family }}-ko-log.tar.gz

  non-primary-cni-e2e:
    name: Non-Primary CNI E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'non-primary-cni-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - name: Remove DNS search domain
        run: |
          sudo sed -i '/^search/d' /etc/resolv.conf
          sudo systemctl restart docker

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: |
          docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Mults-CNI, Cilium, and Kube-OVN
        id: install
        run: make kind-install-multus-cilium-fabric-non-primary-${{ matrix.ip-family }}

      - name: Run non-primary CNI e2e
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          E2E_NETWORK_MODE: overlay
          KUBE_OVN_PRIMARY_CNI: "false"
        run: make fabric-non-primary-cni-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > non-primary-cni-e2e-${{ matrix.ip-family }}-events.yaml
          tar zcf non-primary-cni-e2e-${{ matrix.ip-family }}-events.tar.gz non-primary-cni-e2e-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: non-primary-cni-e2e-${{ matrix.ip-family }}-events
          path: non-primary-cni-e2e-${{ matrix.ip-family }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf non-primary-cni-e2e-${{ matrix.ip-family }}-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: non-primary-cni-e2e-${{ matrix.ip-family }}-audit-log
          path: non-primary-cni-e2e-${{ matrix.ip-family }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz non-primary-cni-e2e-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: non-primary-cni-e2e-${{ matrix.ip-family }}-ko-log
          path: non-primary-cni-e2e-${{ matrix.ip-family }}-ko-log.tar.gz

  fabric-hosted-ovn-central-e2e:
    name: Kube-OVN Hosted OVN Central E2E (${{ matrix.ip-family }}, ${{ matrix.tenant-control-plane }} control-plane)
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-hosted-ovn-central-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    strategy:
      fail-fast: false
      matrix:
        include:
          - ip-family: ipv4
            tenant-control-plane: single
            tenant-control-plane-replicas: 1
          - ip-family: ipv6
            tenant-control-plane: single
            tenant-control-plane-replicas: 1
          - ip-family: dual
            tenant-control-plane: single
            tenant-control-plane-replicas: 1
          - ip-family: ipv4
            tenant-control-plane: ha
            tenant-control-plane-replicas: 3
          - ip-family: ipv6
            tenant-control-plane: ha
            tenant-control-plane-replicas: 3
          - ip-family: dual
            tenant-control-plane: ha
            tenant-control-plane-replicas: 3
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Remove DNS search domain
        run: |
          sudo sed -i '/^search/d' /etc/resolv.conf
          sudo systemctl restart docker

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: |
          docker load --input fabric.tar
          docker tag "ghcr.io/cloudyfolks-labs/fabric:$(cat VERSION)" ghcr.io/cloudyfolks-labs/fabric:dev

      - name: Verify hosted OVN central values
        run: bash hack/kamaji-e2e-test.sh

      - name: Create hosted OVN central environment
        id: install
        env:
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          TENANT_CONTROL_PLANE_REPLICAS: ${{ matrix.tenant-control-plane-replicas }}
        run: make kind-install-kamaji

      - name: Run hosted OVN central E2E
        id: e2e
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          E2E_NETWORK_MODE: overlay
        run: make fabric-kamaji-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl --context kind-mgmt get events -A -o yaml > hcp-e2e-mgmt-events.yaml || true
          tenant_kubeconfig="$(./hack/kamaji-e2e.sh kubeconfig)"
          if [ -s "$tenant_kubeconfig" ]; then
            KUBECONFIG="$tenant_kubeconfig" kubectl get events -A -o yaml > hcp-e2e-tenant-events.yaml || true
          fi
          tar zcf hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-events.tar.gz hcp-e2e-*-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-events
          path: hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-events.tar.gz

      - name: Collect hosted OVN central diagnostics
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          job_dir="${JOB_DIR:-/tmp/kamaji-e2e}"
          if [ -d "$job_dir/diagnostics" ]; then
            tar -C "$job_dir" -zcf hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-diagnostics.tar.gz diagnostics
          fi

      - name: Upload hosted OVN central diagnostics
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-diagnostics
          path: hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-diagnostics.tar.gz
          if-no-files-found: ignore

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          tenant_kubeconfig="$(./hack/kamaji-e2e.sh kubeconfig)"
          if [ -s "$tenant_kubeconfig" ]; then
            KUBECONFIG="$tenant_kubeconfig" make kubectl-ko-log
            mv kubectl-ko-log.tar.gz hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-ko-log.tar.gz
          fi

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-ko-log
          path: hcp-e2e-${{ matrix.ip-family }}-${{ matrix.tenant-control-plane }}-ko-log.tar.gz
          if-no-files-found: ignore

      - name: Clean hosted OVN central environment
        if: always()
        run: make kind-clean-kamaji

  chart-test:
    name: Chart Installation/Uninstallation Test
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'chart-test')
    needs:
      - e2e-selection
      - build-fabric
    runs-on: ubuntu-24.04
    strategy:
      fail-fast: false
      matrix:
        ssl:
          - "true"
          - "false"
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        env:
          ENABLE_SSL: "${{ matrix.ssl }}"
        run: make kind-install-chart

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && steps.install.conclusion == 'failure') }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz chart-test-${{ matrix.ssl }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: chart-test-${{ matrix.ssl }}-ko-log
          path: chart-test-${{ matrix.ssl }}-ko-log.tar.gz

      - name: Uninstall Kube-OVN
        run: make uninstall-chart

  underlay-logical-gateway-installation-test:
    name: Underlay Logical Gateway Installation Test
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'underlay-logical-gateway-installation-test')
    needs:
      - e2e-selection
      - build-fabric
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-dual

      - name: Install Kube-OVN
        id: install
        run: make kind-install-underlay-logical-gateway-dual

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && steps.install.conclusion == 'failure') }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz underlay-logical-gateway-installation-test-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: underlay-logical-gateway-installation-test-ko-log
          path: underlay-logical-gateway-installation-test-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  no-ovn-lb-test:
    name: Disable OVN LB Test
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'no-ovn-lb-test')
    needs:
      - e2e-selection
      - build-fabric
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN without LoadBalancer
        id: install
        env:
          ENABLE_LB: "false"
        run: make kind-install

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && steps.install.conclusion == 'failure') }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz no-ovn-lb-test-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: no-ovn-lb-test-ko-log
          path: no-ovn-lb-test-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  no-np-test:
    name: Disable Network Policy Test
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'no-np-test')
    needs:
      - e2e-selection
      - build-fabric
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        env:
          ENABLE_NP: "false"
        run: make kind-install

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && steps.install.conclusion == 'failure') }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz no-np-test-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: no-np-test-ko-log
          path: no-np-test-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  webhook-e2e:
    name: Webhook E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'webhook-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: |
          docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN with webhook
        id: install
        run: make kind-install-webhook

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make fabric-webhook-e2e


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz webhook-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: webhook-e2e-ko-log
          path: webhook-e2e-ko-log.tar.gz

  installation-compatibility-test:
    name: Installation Compatibility Test
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'installation-compatibility-test')
    needs:
      - e2e-selection
      - build-fabric
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    strategy:
      fail-fast: false
      matrix:
        k8s-version:
          - v1.29.14
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
          K8S_VERSION: ${{ matrix.k8s-version }}
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        run: make kind-install

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && steps.install.conclusion == 'failure') }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz installation-compatibility-test-${{ matrix.k8s-version }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: installation-compatibility-test-${{ matrix.k8s-version }}-ko-log
          path: installation-compatibility-test-${{ matrix.k8s-version }}-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  fabric-cnp-domain-e2e:
    name: Kube-OVN CNP Domain E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-cnp-domain-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
        mode:
          - overlay
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Set environment variables
        run: |
          if [ $(($RANDOM%2)) -ne 0 ]; then
            # run as root and use valgrind to debug memory leak
            echo "VERSION=$(cat VERSION)-debug" >> "$GITHUB_ENV"
            echo "DEBUG_WRAPPER=valgrind" >> "$GITHUB_ENV"
          fi

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-anp

      - name: Run CNP Domain E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          E2E_NETWORK_MODE: ${{ matrix.mode }}
        run: make fabric-cnp-domain-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml
          tar zcf fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events
          path: fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log
          path: fabric-cnp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

  fabric-anp-domain-e2e:
    name: Kube-OVN ANP Domain E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-anp-domain-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
        mode:
          - overlay
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Set environment variables
        run: |
          if [ $(($RANDOM%2)) -ne 0 ]; then
            # run as root and use valgrind to debug memory leak
            echo "VERSION=$(cat VERSION)-debug" >> "$GITHUB_ENV"
            echo "DEBUG_WRAPPER=valgrind" >> "$GITHUB_ENV"
          fi

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-anp

      - name: Run ANP Domain E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          E2E_NETWORK_MODE: ${{ matrix.mode }}
        run: make fabric-anp-domain-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml
          tar zcf fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events
          path: fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log
          path: fabric-anp-domain-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

  cilium-chaining-e2e:
    name: Cilium Chaining E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'cilium-chaining-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
          # - ipv6
          # - dual
        mode:
          - overlay
          - underlay
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false
      - uses: azure/setup-helm@v5.0.1
        with:
          version: '${{ env.HELM_VERSION }}'

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - name: Configure docker
        run: |
          # enable addition of ip6tables rules
          sudo sh -c "echo '{\"experimental\": true, \"ip6tables\": true}' > /etc/docker/daemon.json"
          # remove DNS search domain
          sudo sed -i '/^search/d' /etc/resolv.conf
          # restart docker
          sudo systemctl restart docker

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-cilium-chaining-${{ matrix.ip-family }}

      - name: Install Kube-OVN with Cilium chaining
        id: install
        run: make kind-install-cilium-chaining-${{ matrix.mode }}-${{ matrix.ip-family }}

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_CILIUM_CHAINING: "true"
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
          E2E_NETWORK_MODE: ${{ matrix.mode }}
        run: make k8s-conformance-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml
          tar zcf cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events
          path: cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-audit-log
          path: cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log
          path: cilium-chaining-e2e-${{ matrix.mode }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  fabric-ha-e2e:
    name: Kube-OVN HA E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-ha-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ssl:
          - "true"
          - "false"
        bind-local:
          - "true"
          - "false"
        ip-family:
          - ipv4
          - ipv6
          - dual
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-ha-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        env:
          NET_STACK: "${{ matrix.ip-family }}"
          ENABLE_SSL: "${{ matrix.ssl }}"
          ENABLE_BIND_LOCAL_IP: "${{ matrix.bind-local }}"
          TLS_MIN_VERSION: "${{ matrix.ssl == 'true' && 'TLS12' || '' }}"
          TLS_MAX_VERSION: "${{ matrix.ssl == 'true' && 'TLS13' || '' }}"
          TLS_CIPHER_SUITES: "${{ matrix.ssl == 'true' && 'TLS_ECDHE_RSA_WITH_AES_256_GCM_SHA384,TLS_AES_256_GCM_SHA384' || '' }}"
        run: make kind-install-chart

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
        run: |
          make fabric-security-e2e
          make fabric-ha-e2e


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-ha-e2e-${{ matrix.ssl }}-${{ matrix.bind-local }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-ha-e2e-${{ matrix.ssl }}-${{ matrix.bind-local }}-${{ matrix.ip-family }}-ko-log
          path: fabric-ha-e2e-${{ matrix.ssl }}-${{ matrix.bind-local }}-${{ matrix.ip-family }}-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  fabric-submariner-conformance-e2e:
    name: Kube-OVN Submariner Conformance E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-submariner-conformance-e2e')
    needs:
      - e2e-selection
      - build-fabric
    runs-on: ubuntu-24.04
    timeout-minutes: 60
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Install submariner subctl
        env:
          VERSION: v${{ env.SUBMARINER_VERSION }}
          DESTDIR: /usr/local/bin
        run: curl -Ls https://get.submariner.io | bash

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init-ovn-submariner

      - name: Install Kube-OVN and Submariner
        id: install
        run: make kind-install-ovn-submariner

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        run: make fabric-submariner-conformance-e2e


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-submariner-conformance-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-submariner-conformance-e2e-ko-log
          path: fabric-submariner-conformance-e2e-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  vpc-dynamic-routing-e2e:
    name: VPC Dynamic Routing E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'vpc-dynamic-routing-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 1
          path: test/e2e/source
          persist-credentials: false

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load image
        run: docker load --input fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          n_worker=2 make kind-init-ipv4

      - name: Install Kube-OVN
        id: install
        env:
          UNTAINT_CONTROL_PLANE: "false"
          ENABLE_OVN_LB_SVC: "true"
        run: make kind-install-ipv4

      - name: Load the vrf kernel module
        run: |
          sudo modprobe vrf || {
            sudo apt-get update -qq
            sudo apt-get install -y --no-install-recommends "linux-modules-extra-$(uname -r)"
            sudo modprobe vrf
          }

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ipv4
        run: make vpc-dynamic-routing-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > vpc-dynamic-routing-e2e-events.yaml
          tar zcf vpc-dynamic-routing-e2e-events.tar.gz vpc-dynamic-routing-e2e-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: vpc-dynamic-routing-e2e-events
          path: vpc-dynamic-routing-e2e-events.tar.gz

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz vpc-dynamic-routing-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: vpc-dynamic-routing-e2e-ko-log
          path: vpc-dynamic-routing-e2e-ko-log.tar.gz

      - name: Cleanup
        run: timeout -k 10 180 sh -x dist/images/cleanup.sh

  ovn-vpc-nat-gw-conformance-e2e:
    name: OVN VPC NAT Gateway E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'ovn-vpc-nat-gw-conformance-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        run: make kind-install

      - name: Run Vip E2E
        id: vip-e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make vip-conformance-e2e

      - name: Run Ovn VPC NAT GW E2E
        id: vpc-e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make ovn-vpc-nat-gw-conformance-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.vip-e2e.conclusion == 'failure' || steps.vpc-e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > ovn-vpc-nat-gw-conformance-e2e-events.yaml
          tar zcf ovn-vpc-nat-gw-conformance-e2e-events.tar.gz ovn-vpc-nat-gw-conformance-e2e-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.vip-e2e.conclusion == 'failure' || steps.vpc-e2e.conclusion == 'failure')
        with:
          name: ovn-vpc-nat-gw-conformance-e2e-events
          path: ovn-vpc-nat-gw-conformance-e2e-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.vip-e2e.conclusion == 'failure' || steps.vpc-e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf ovn-vpc-nat-gw-conformance-e2e-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.vip-e2e.conclusion == 'failure' || steps.vpc-e2e.conclusion == 'failure')
        with:
          name: ovn-vpc-nat-gw-conformance-e2e-audit-log
          path: ovn-vpc-nat-gw-conformance-e2e-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.vip-e2e.conclusion == 'failure' || steps.vpc-e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.vip-e2e.conclusion == 'failure' || steps.vpc-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz ovn-vpc-nat-gw-conformance-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.vip-e2e.conclusion == 'failure' || steps.vpc-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: ovn-vpc-nat-gw-conformance-e2e-ko-log
          path: ovn-vpc-nat-gw-conformance-e2e-ko-log.tar.gz

  fabric-ipsec-e2e:
    name: OVN IPSEC E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-ipsec-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        run: make kind-install-ovn-ipsec

      - name: Run Ovn IPSEC E2E
        id: fabric-ipsec-e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make fabric-ipsec-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > fabric-ipsec-e2e-events.yaml
          tar zcf fabric-ipsec-e2e-events.tar.gz fabric-ipsec-e2e-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-e2e.conclusion == 'failure')
        with:
          name: fabric-ipsec-e2e-events
          path: fabric-ipsec-e2e-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf fabric-ipsec-e2e-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-e2e.conclusion == 'failure')
        with:
          name: fabric-ipsec-e2e-audit-log
          path: fabric-ipsec-e2e-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-e2e.conclusion == 'failure')) }}
        env:
          IGNORABLE_PODS: app=fabric-pinger
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-ipsec-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-ipsec-e2e-ko-log
          path: fabric-ipsec-e2e-ko-log.tar.gz

  fabric-ipsec-cert-mgr-e2e:
    name: OVN IPSEC E2E CERT MANAGER
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-ipsec-cert-mgr-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        run: make kind-install-ovn-ipsec-cert-manager

      - name: Run Ovn IPSEC cert-manager E2E
        id: fabric-ipsec-cert-mgr-e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make fabric-ipsec-cert-mgr-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-cert-mgr-e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > fabric-ipsec-cert-mgr-e2e-events.yaml
          tar zcf fabric-ipsec-cert-mgr-e2e-events.tar.gz fabric-ipsec-cert-mgr-e2e-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-cert-mgr-e2e.conclusion == 'failure')
        with:
          name: fabric-ipsec-cert-mgr-e2e-events
          path: fabric-ipsec-cert-mgr-e2e-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-cert-mgr-e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf fabric-ipsec-cert-mgr-e2e-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-cert-mgr-e2e.conclusion == 'failure')
        with:
          name: fabric-ipsec-cert-mgr-e2e-audit-log
          path: fabric-ipsec-cert-mgr-e2e-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-cert-mgr-e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-cert-mgr-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-ipsec-cert-mgr-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-ipsec-cert-mgr-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-ipsec-cert-mgr-e2e-ko-log
          path: fabric-ipsec-cert-mgr-e2e-ko-log.tar.gz

  fabric-connectivity-test:
    name: Kube-OVN Connectivity E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-connectivity-test')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        mode:
          - overlay
          - underlay
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        run: make kind-install-${{ matrix.mode }}

      - name: Run E2E
        id: e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make fabric-connectivity-e2e

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-connectivity-e2e-${{ matrix.mode }}-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.e2e.conclusion == 'failure')
        with:
          name: fabric-connectivity-e2e-${{ matrix.mode }}-ko-log
          path: fabric-connectivity-e2e-${{ matrix.mode }}-ko-log.tar.gz


  fabric-underlay-metallb-e2e:
    name: OVN METALLB E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'fabric-underlay-metallb-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    strategy:
      fail-fast: false
      matrix:
        ip-family:
          - ipv4
          - ipv6
          - dual
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          n_worker=2 make kind-init-${{ matrix.ip-family }}

      - name: Install Kube-OVN
        id: install
        run: make kind-install-metallb-pool-from-underlay-${{ matrix.ip-family }}

      - name: Run Ovn Metallb and Kube-OVN Combine E2E
        id: fabric-underlay-metallb-e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
          E2E_IP_FAMILY: ${{ matrix.ip-family }}
        run: make fabric-underlay-metallb-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-underlay-metallb-e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-events.yaml
          tar zcf fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-events.tar.gz fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-underlay-metallb-e2e.conclusion == 'failure')
        with:
          name: fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-events
          path: fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-underlay-metallb-e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-underlay-metallb-e2e.conclusion == 'failure')
        with:
          name: fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-audit-log
          path: fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-audit-log.tar.gz


      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.fabric-underlay-metallb-e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-underlay-metallb-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.fabric-underlay-metallb-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-ko-log
          path: fabric-underlay-metallb-${{ matrix.ip-family }}-e2e-ko-log.tar.gz

  router-lb-rule-e2e:
    name: Router LB Rule E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'router-lb-rule-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        run: make kind-install

      - name: Run Router LB Rule E2E
        id: rlr-e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make fabric-rlr-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > router-lb-rule-e2e-events.yaml
          tar zcf router-lb-rule-e2e-events.tar.gz router-lb-rule-e2e-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        with:
          name: router-lb-rule-e2e-events
          path: router-lb-rule-e2e-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf router-lb-rule-e2e-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        with:
          name: router-lb-rule-e2e-audit-log
          path: router-lb-rule-e2e-audit-log.tar.gz

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz router-lb-rule-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: router-lb-rule-e2e-ko-log
          path: router-lb-rule-e2e-ko-log.tar.gz

  dns-zone-e2e:
    name: DNS Zone E2E
    if: github.event_name != 'workflow_dispatch' || contains(fromJSON(needs.e2e-selection.outputs.executionJobIds), 'dns-zone-e2e')
    needs:
      - e2e-selection
      - build-fabric
      - build-e2e-binaries
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Free disk space
        run: sudo rm -rf /usr/local/lib/android /usr/share/dotnet /opt/ghc /usr/local/.ghcup

      - uses: actions/checkout@v7
        with:
          ref: ${{ inputs.headSHA || github.sha }}
          persist-credentials: false

      - name: Create the default branch directory
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        run: mkdir -p test/e2e/source

      - name: Check out the default branch
        if: (github.base_ref || github.ref_name) != github.event.repository.default_branch
        uses: actions/checkout@v7
        with:
          ref: ${{ github.event.repository.default_branch }}
          persist-credentials: false
          fetch-depth: 1
          path: test/e2e/source

      - name: Export E2E directory
        run: |
          if [ '${{ github.base_ref || github.ref_name }}' = '${{ github.event.repository.default_branch }}' ]; then
            echo "E2E_DIR=." >> "$GITHUB_ENV"
          else
            echo "E2E_DIR=test/e2e/source" >> "$GITHUB_ENV"
          fi

      - uses: actions/setup-go@v7
        id: setup-go
        with:
          go-version-file: ${{ env.E2E_DIR }}/go.mod
          check-latest: true
          cache: false

      - name: Export Go full version
        run: echo "GO_VERSION=${{ steps.setup-go.outputs.go-version }}" >> "$GITHUB_ENV"

      - name: Go cache
        uses: actions/cache/restore@v6
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-${{ hashFiles(format('{0}/**/go.sum', env.E2E_DIR)) }}
          restore-keys: |
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.ref_name }}-
            ${{ runner.os }}-e2e-go-${{ env.GO_VERSION }}-x86-${{ github.base_ref }}-

      - name: Install kind
        uses: helm/kind-action@v1.14.0
        with:
          version: ${{ env.KIND_VERSION }}
          install_only: true

      - name: Download fabric image
        uses: actions/download-artifact@v8
        with:
          name: fabric

      - name: Load images
        run: docker load -i fabric.tar

      - name: Create kind cluster
        run: |
          pipx install jinjanator
          make kind-init

      - name: Install Kube-OVN
        id: install
        run: make kind-install

      - name: Run DNS Zone E2E
        id: rlr-e2e
        working-directory: ${{ env.E2E_DIR }}
        env:
          E2E_BRANCH: ${{ github.base_ref || github.ref_name }}
        run: make fabric-dns-zone-e2e

      - name: Collect k8s events
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        run: |
          kubectl get events -A -o yaml > dns-zone-e2e-events.yaml
          tar zcf dns-zone-e2e-events.tar.gz dns-zone-e2e-events.yaml

      - name: Upload k8s events
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        with:
          name: dns-zone-e2e-events
          path: dns-zone-e2e-events.tar.gz

      - name: Collect apiserver audit logs
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        run: |
          docker cp fabric-control-plane:/var/log/kubernetes/kube-apiserver-audit.log .
          tar zcf dns-zone-e2e-audit-log.tar.gz kube-apiserver-audit.log

      - name: Upload apiserver audit logs
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')
        with:
          name: dns-zone-e2e-audit-log
          path: dns-zone-e2e-audit-log.tar.gz

      - name: Check kube ovn pod restarts
        id: check-restarts
        if: ${{ success() || (failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure')) }}
        run: make check-fabric-pod-restarts

      - name: kubectl ko log
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        run: |
          make kubectl-ko-log
          mv kubectl-ko-log.tar.gz dns-zone-e2e-ko-log.tar.gz

      - name: upload kubectl ko log
        uses: actions/upload-artifact@v7
        if: failure() && (steps.install.conclusion == 'failure' || steps.rlr-e2e.conclusion == 'failure' || steps.check-restarts.conclusion == 'failure')
        with:
          name: dns-zone-e2e-ko-log
          path: dns-zone-e2e-ko-log.tar.gz


  e2e-executor-result:
    name: x86 E2E Executor Result
    if: github.event_name == 'workflow_dispatch' && always()
    permissions: {}
    needs:
      - e2e-selection
      - chart-test
      - cilium-chaining-e2e
      - cyclonus-netpol-e2e
      - installation-compatibility-test
      - k8s-conformance-e2e
      - k8s-netpol-e2e
      - fabric-anp-domain-e2e
      - fabric-cnp-domain-e2e
      - fabric-conformance-e2e
      - fabric-connectivity-test
      - fabric-ha-e2e
      - fabric-hosted-ovn-central-e2e
      - fabric-ic-conformance-e2e
      - fabric-ipsec-cert-mgr-e2e
      - fabric-ipsec-e2e
      - fabric-submariner-conformance-e2e
      - fabric-underlay-metallb-e2e
      - multus-conformance-e2e
      - no-np-test
      - no-ovn-lb-test
      - non-primary-cni-e2e
      - ovn-vpc-nat-gw-conformance-e2e
      - dns-zone-e2e
      - router-lb-rule-e2e
      - underlay-logical-gateway-installation-test
      - vpc-dynamic-routing-e2e
      - webhook-e2e
    runs-on: ubuntu-24.04
    env:
      NEEDS_JSON: ${{ toJSON(needs) }}
      SELECTED_JOB_IDS: ${{ needs.e2e-selection.outputs.executionJobIds }}
    steps:
      - name: Verify every selected test Job succeeded
        run: |
          python3 - <<'PY'
          import json
          import os

          needs = json.loads(os.environ["NEEDS_JSON"])
          selectedJobIds = json.loads(os.environ["SELECTED_JOB_IDS"])
          incomplete = {
              jobId: needs.get(jobId, {}).get("result", "missing")
              for jobId in selectedJobIds
              if needs.get(jobId, {}).get("result") != "success"
          }
          if incomplete:
              raise SystemExit(f"selected x86 E2E Jobs did not succeed: {incomplete}")
          print(f"Verified {len(selectedJobIds)} selected x86 E2E Jobs.")
          PY
