{{- if include "fabric.renderDataPlane" . -}}
kind: Service
apiVersion: v1
metadata:
  name: fabric-pinger
  namespace: {{ .Values.namespace }}
  {{- with .Values.pinger.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    app.kubernetes.io/name: fabric-pinger
    app.kubernetes.io/part-of: fabric
    {{- with .Values.pinger.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  selector:
    app.kubernetes.io/name: fabric-pinger
    app.kubernetes.io/part-of: fabric
  ports:
    - name: metrics
      port: {{ .Values.pinger.metrics.port }}
  {{- if eq .Values.networking.stack "Dual" }}
  ipFamilyPolicy: PreferDualStack
  {{- end }}
{{- end }}
