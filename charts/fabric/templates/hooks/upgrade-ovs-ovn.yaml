{{- if and (include "fabric.renderFullOnly" .) .Values.ovsOvn.upgrade.enabled -}}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: ovs-ovn-upgrade
  namespace: {{ .Values.namespace }}
  annotations:
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": post-upgrade
    "helm.sh/hook-weight": "1"
    "helm.sh/hook-delete-policy": hook-succeeded
automountServiceAccountToken: false
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  annotations:
    rbac.authorization.k8s.io/system-only: "true"
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": post-upgrade
    "helm.sh/hook-weight": "2"
    "helm.sh/hook-delete-policy": hook-succeeded
  name: system:ovs-ovn-upgrade
rules:
  - apiGroups:
      - apps
    resources:
      - daemonsets
    verbs:
      - list
      - get
      - watch
  - apiGroups:
      - apps
    resources:
      - daemonsets
    resourceNames:
      - ovs-ovn
    verbs:
      - get
  - apiGroups:
      - apps
    resources:
      - deployments
    resourceNames:
      - ovn-central
    verbs:
      - get
      - list
      - watch
  - apiGroups:
      - ""
    resources:
      - nodes
    verbs:
      - list
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - list
      - get
      - watch
      - delete
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: ovs-ovn-upgrade
  annotations:
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": post-upgrade
    "helm.sh/hook-weight": "3"
    "helm.sh/hook-delete-policy": hook-succeeded
roleRef:
  name: system:ovs-ovn-upgrade
  kind: ClusterRole
  apiGroup: rbac.authorization.k8s.io
subjects:
  - kind: ServiceAccount
    name: ovs-ovn-upgrade
    namespace: {{ .Values.namespace }}
---
apiVersion: batch/v1
kind: Job
metadata:
  name: "{{ .Chart.Name }}-post-upgrade-hook"
  namespace: {{ .Values.namespace }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
    app.kubernetes.io/instance: {{ .Release.Name | quote }}
    app.kubernetes.io/version: {{ .Chart.AppVersion }}
    helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
  annotations:
    # This is what defines this resource as a hook. Without this line, the
    # job is considered part of the release.
    "helm.sh/hook": post-upgrade
    "helm.sh/hook-weight": "4"
    "helm.sh/hook-delete-policy": hook-succeeded
spec:
  completions: 1
  template:
    metadata:
      name: "{{ .Release.Name }}"
      labels:
        app.kubernetes.io/managed-by: {{ .Release.Service | quote }}
        app.kubernetes.io/instance: {{ .Release.Name | quote }}
        helm.sh/chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
        app: post-upgrade
        component: job
    spec:
      tolerations:
        - key: ""
          operator: "Exists"
          effect: "NoSchedule"
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - topologyKey: kubernetes.io/hostname
              labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - post-upgrade
                  - key: component
                    operator: In
                    values:
                      - job
      restartPolicy: Never
      hostNetwork: true
      nodeSelector:
        kubernetes.io/os: "linux"
      serviceAccountName: ovs-ovn-upgrade
      automountServiceAccountToken: true
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: ovs-ovn-upgrade
          image: "{{ .Values.global.registry.address}}/{{ .Values.global.images.fabric.repository }}:{{ .Values.global.images.fabric.tag }}"
          env:
            - name: POD_NAMESPACE
              valueFrom:
                fieldRef:
                  fieldPath: metadata.namespace
            - name: ENABLE_SSL
              value: "{{ .Values.networking.enableSsl }}"
            {{- if .Values.central.hcp.enabled }}
            - name: OVN_NB_ADDR
              value: "{{ include "fabric.ovnNbAddress" . }}"
            {{- else }}
            - name: OVN_DB_IPS
              value: "{{ include "fabric.masterNodes" . | default (include "fabric.nodeIPs" .) }}"
            {{- end }}
            - name: OVN_VERSION_COMPATIBILITY
              value: {{ .Values.ovsOvn.upgrade.versionCompatibility | quote }}
          command:
            - bash
            - -eo
            - pipefail
            - -c
            - |
              touch /var/log/fabric/upgrade-ovs.log
              chmod 640 /var/log/fabric/upgrade-ovs.log
              /fabric/upgrade-ovs.sh 2>&1 | tee -a /var/log/fabric/upgrade-ovs.log
          volumeMounts:
            - mountPath: /var/log/fabric
              name: fabric-log
            - mountPath: /var/run/tls
              name: fabric-tls
      volumes:
        - name: fabric-log
          hostPath:
            path: {{ .Values.logging.directory }}/fabric
        - name: fabric-tls
          secret:
            optional: true
            secretName: fabric-tls
{{- end -}}
